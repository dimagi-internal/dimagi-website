# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Python
__pycache__/
*.py[cod]

# Node
node_modules/
package-lock.json

# Wrangler / Cloudflare local state
.wrangler/
.dev.vars

# Build context: track the build scripts + JSON data, but exclude the heavy raw
# import dumps (the WordPress export and the scraped-article archive) and the
# duplicate source image already copied into assets/.
Context/*.xml
Context/blog-import/records_archive/
Context/Podcast-Cover.jpg

# Cloudflare deploy output
dist/
